<Window x:Class="TacoBell.Views.MenuPage"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helpers="clr-namespace:TacoBell.Helpers"
        mc:Ignorable="d"
        Title="MenuPage" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#5C2D73">

    <Window.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <helpers:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <helpers:BoolToTextConverter x:Key="BoolToTextConverter"/>
        <helpers:SwitchModeTextConverter x:Key="SwitchModeTextConverter"/>
        <helpers:ImagePathConverter x:Key="ImagePathConverter"/>
    </Window.Resources>

    <Grid>
        <!-- Top Black Bar -->
        <Grid Background="Black" Height="80" VerticalAlignment="Top" Panel.ZIndex="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <Ellipse Width="40" Height="40" Fill="White" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                            Visibility="{Binding IsUserLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding CurrentUserName}" Foreground="White" FontSize="16" VerticalAlignment="Center" Margin="5"/>
                    <Button Content="Logout"
                            Command="{Binding LogoutCommand}"
                            Background="Transparent" Foreground="White" BorderThickness="0" Margin="10,0,0,0"/>
                </StackPanel>

                <Button Content="Login / Sign up"
                        Style="{StaticResource HoverTextButtonStyle}"
                        Command="{Binding NavigateToLoginCommand}"
                        VerticalAlignment="Center" Margin="10,0,0,0"
                        Visibility="{Binding IsUserLoggedIn, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
            </StackPanel>

            <Image Grid.Column="1" Source="/Assets/Images/logo.png" Height="50" VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <Button Grid.Column="2" Command="{Binding OpenAppMenuCommand}" Background="Transparent" BorderThickness="0" Cursor="Hand"
                    HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0" Padding="0">
                <StackPanel Orientation="Vertical">
                    <Rectangle Height="4" Width="25" Fill="White" Margin="0,3"/>
                    <Rectangle Height="4" Width="25" Fill="White" Margin="0,3"/>
                    <Rectangle Height="4" Width="25" Fill="White" Margin="0,3"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- App Menu -->
        <Border Background="#CC2E0854" CornerRadius="0,0,12,12" Padding="5" Height="220" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,80,20,0"
                Visibility="{Binding IsAppMenuVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                Panel.ZIndex="2">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="40">
                <Button Content="HOME" Command="{Binding NavigateToHomeCommand}" Style="{StaticResource MenuButtonStyle}" Margin="8"/>
                <Button Content="MENU" Command="{Binding NavigateToMenuCommand}" Style="{StaticResource MenuButtonStyle}" Margin="8"/>
                <Button Content="ACCOUNT" Command="{Binding NavigateToAccountCommand}" Style="{StaticResource MenuButtonStyle}" Margin="8"/>
            </StackPanel>
        </Border>

        <!-- Scrollable Content -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,80,0,0">
            <StackPanel Margin="20">

                <!-- Categorii -->
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" 
                                        Style="{StaticResource RoundedCategoryButtonStyle}"
                                        Command="{Binding DataContext.SelectCategoryCommand, ElementName=RootWindow}"
                                        CommandParameter="{Binding}"/>

                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>

                <Button Content="Vezi Coș" 
                    Command="{Binding ToggleCartCommand}" 
                    HorizontalAlignment="Right"
                    Margin="0,0,20,10"
                    Style="{StaticResource RoundedCategoryButtonStyle}" />

                <!-- Carduri -->
                <ItemsControl ItemsSource="{Binding FilteredItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource MenuCardStyle}">
                                <StackPanel>
                                    <Border CornerRadius="10" ClipToBounds="True" Margin="0,0,0,10">
                                        <Image Source="{Binding ImagePath, Converter={StaticResource ImagePathConverter}}"
                                               Height="160" Stretch="UniformToFill"/>
                                    </Border>

                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="18" Margin="0,5"/>
                                    <TextBlock Text="{Binding PortionInfo}" FontSize="14"/>
                                    <TextBlock Text="{Binding PriceInfo}" FontSize="14" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Availability}" Foreground="Red" FontWeight="Bold" FontSize="13"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Center">
                                        <Button Content="Alergeni" 
                                                Style="{StaticResource RoundedActionButtonStyle}"
                                                Command="{Binding DataContext.ShowAllergensCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                CommandParameter="{Binding}"/>
                                        <Button Content="Adaugă" 
                                                Style="{StaticResource RoundedActionButtonStyle}"
                                                IsEnabled="{Binding IsAvailable}"
                                                Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"/>

                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
        <Border Background="WhiteSmoke"
            BorderBrush="Black"
            BorderThickness="2"
            Padding="10"
            CornerRadius="8"
            Width="300"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Visibility="{Binding IsCartVisible, Converter={StaticResource BoolToVisibilityConverter}}"
            Margin="20">
                <StackPanel>
                    <TextBlock Text="Coșul de cumpărături" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
                    <ItemsControl ItemsSource="{Binding CartItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="60" />
                                        <ColumnDefinition Width="60" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center"/>

                                    <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center">
                                        <Button Content="▲"
                                        FontWeight="Bold"
                                        Padding="0"
                                        Width="25"
                                        Height="20"
                                        Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}" />
                                        <TextBlock Text="{Binding Quantity}" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <Button Content="▼"
                                        FontWeight="Bold"
                                        Padding="0"
                                        Width="25"
                                        Height="20"
                                        Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}" />
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="{Binding TotalPrice, StringFormat={}{0:0.00} lei}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                <Separator Margin="0,10"/>
                <StackPanel Visibility="{Binding CartItems.Count, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Separator Margin="0,10"/>
                    <TextBlock Text="{Binding Subtotal, StringFormat='Subtotal: {0:0.00} lei'}" />
                    <TextBlock Text="{Binding ShippingFee, StringFormat='Transport: {0:0.00} lei'}" />
                    <TextBlock Text="{Binding Discount, StringFormat='Reducere: {0:0.00} lei'}" />
                    <TextBlock Text="{Binding Total, StringFormat='Total: {0:0.00} lei'}" FontWeight="Bold" />
                </StackPanel>

                <Button Content="Plasează Comanda"
                    Command="{Binding PlaceOrderCommand}"
                    IsEnabled="{Binding CartItems.Count, Converter={StaticResource BoolToVisibilityConverter}}"
                    Style="{StaticResource RoundedActionButtonStyle}"
                    Margin="0,10,0,0"/>

            </StackPanel>
        </Border>


    </Grid>
</Window>
